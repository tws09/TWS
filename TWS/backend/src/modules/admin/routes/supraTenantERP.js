const express = require('express');
const router = express.Router();

// All ERP management routes have been removed
// ERP configuration should be handled during tenant creation/editing in Tenant Management

module.exports = router;
