# Supra-Admin Backend Environment Configuration
# Copy this file to .env and update the values

# Server Configuration
NODE_ENV=development
# Default 5000; on Railway/Heroku/Render the platform sets PORT automatically
PORT=5000
BASE_URL=http://localhost:5000

# Database Configuration
MONGO_URI=mongodb://localhost:27017/tws

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_DISABLED=false
BULLMQ_DISABLED=false

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Security Configuration
ENCRYPTION_MASTER_KEY=your-encryption-master-key-here
BCRYPT_ROUNDS=12

# Email Configuration (Resend - https://resend.com)
RESEND_API_KEY=

# Gemini Nucleus central agent (server-side only)
GEMINI_API_KEY=
GEMINI_CENTRAL_AGENT_MODEL=gemini-3.6-flash
AI_AGENT_DAILY_TOKEN_BUDGET=50000
EMAIL_FROM=noreply@updates.housesbase.com

# Monitoring Configuration
LOG_LEVEL=info
METRICS_PORT=9090
ENABLE_METRICS=true

# Alerting Configuration
ALERT_EMAIL_RECIPIENTS=admin@example.com

# External Services
EMAIL_SERVICE_URL=http://localhost:3001/health

# Feature Flags
ENABLE_NOTIFICATIONS=true
ENABLE_EMAIL_SERVICE=true
ENABLE_AWS=false
ENABLE_FIREBASE=false

# Query-time tenant isolation (WP1). Enforced UNLESS set to exactly "false".
# When "false", the tenantScope Mongoose plugin only logs what it *would* have
# scoped and leaves queries unmodified (use only for a controlled rollback).
TENANT_SCOPE_ENFORCE=true

# Scheduled job kill switches — set to "false" to disable, anything else (or unset) keeps them on
JOBS_INVOICE_GENERATION_ENABLED=true
JOBS_TENANT_HEALTH_EMAILS_ENABLED=true

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
CORS_ORIGIN=http://localhost:4000
SOCKET_CORS_ORIGIN=http://localhost:4000
FRONTEND_URL=http://localhost:4000

# Canonical app host — bare hostname only, no protocol, no trailing slash.
# Used for CORS and for building workspace links in emails. Tenancy is
# PATH-based: tenant "acme" lives at housesbase.com/acme/org/... (no subdomains).
BASE_DOMAIN=housesbase.com
